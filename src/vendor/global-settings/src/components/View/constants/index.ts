import { rosetta } from '@sqs/rosetta-themes';

const { sizes } = rosetta.default;

export const DESKTOP_NAVIGATION_WIDTH = '257px';
export const DESKTOP_HEADER_HEIGHT = sizes.sizes[350];
export const MOBILE_HEADER_HEIGHT = sizes.sizes[450];

